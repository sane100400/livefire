# 행사 당일 이 파일을 .env 로 복사 후 실제 값으로 교체
# cp .env.example .env && vim .env

# ── 필수 시크릿 (없으면 서버 시작 시 KeyError 크래시) ─────────────────
ADMIN_SECRET=replace_before_event
RUNNER_SECRET=replace_before_event
TOKEN_TEAM_A=replace_before_event
TOKEN_TEAM_B=replace_before_event
TOKEN_TEAM_C=replace_before_event
TOKEN_TEAM_D=replace_before_event
TOKEN_TEAM_E=replace_before_event
TOKEN_TEAM_F=replace_before_event
TOKEN_TEAM_G=replace_before_event

# ── 디펜스 에이전트 전용 토큰 (오프라인 참가자에게만 배포) ───────────
# 운영에서는 반드시 팀 토큰과 다른 값으로 설정
# DEFENSE_TOKEN_TEAM_A=replace_before_event
# DEFENSE_TOKEN_TEAM_B=replace_before_event
# DEFENSE_TOKEN_TEAM_C=replace_before_event
# DEFENSE_TOKEN_TEAM_D=replace_before_event
# DEFENSE_TOKEN_TEAM_E=replace_before_event
# DEFENSE_TOKEN_TEAM_F=replace_before_event
# DEFENSE_TOKEN_TEAM_G=replace_before_event

# ── 팀 서비스 IP/포트 (기본값: teamA~G = 10.89.21.10-16:8000) ─────
# TEAM_SUFFIXES=A,B,C,D,E,F,G
# IP_TEAM_A=10.89.21.10
# IP_TEAM_B=10.89.21.11
# IP_TEAM_C=10.89.21.12
# IP_TEAM_D=10.89.21.13
# IP_TEAM_E=10.89.21.14
# IP_TEAM_F=10.89.21.15
# IP_TEAM_G=10.89.21.16
# PORT_TEAM_A=8000
# PORT_TEAM_B=8000
# PORT_TEAM_C=8000
# PORT_TEAM_D=8000
# PORT_TEAM_E=8000
# PORT_TEAM_F=8000
# PORT_TEAM_G=8000
# HOST_PORT_TEAM_A=8001
# HOST_PORT_TEAM_B=8002
# HOST_PORT_TEAM_C=8003
# HOST_PORT_TEAM_D=8004
# HOST_PORT_TEAM_E=8005
# HOST_PORT_TEAM_F=8006
# HOST_PORT_TEAM_G=8007

# ── 경로 (기본값 사용 시 주석 처리 유지) ─────────────────────────────
# VULN_SPEC_DIR=/app/vuln_specs
# REPOS_DIR=/app/repos
# DB_PATH=/app/data/game_state.db
# COORDINATOR_URL=http://localhost:9000
# HOOK_COORDINATOR_URL=http://127.0.0.1:9000

# ── OpenRouter gateway (coordinator만 보유) ──────────────────────────
OPENROUTER_API_KEY=replace_before_event
OPENROUTER_BASE_URL=https://openrouter.ai/api/v1

# ── PoC 저장/실행 ───────────────────────────────────────────────────
# DATA_DIR=/app/data
# POC_TIMEOUT_SEC=20
# POC_MAX_BYTES=65536
# POC_OUTPUT_MAX_BYTES=65536
# POC_RUNNER_MODE=auto
# POC_DOCKER_NETWORK=hackathon_target-net
# POC_DOCKER_IMAGE=python:3.11-slim
# Docker socket 모드에서 host daemon이 마운트할 DATA_DIR 절대경로.
# docker-compose.yml은 기본값으로 ${PWD}/data를 넘긴다.
# POC_HOST_DATA_DIR=/absolute/path/to/hackathon/data

# ── 보안 기본값 ─────────────────────────────────────────────────────
# 운영에서는 아래 unsafe 옵션을 켜지 않는다.
# ALLOW_UNSAFE_AGENT_RUNS=0
# ALLOW_STUDENT_AGENT_RUNS=0
# MAX_LLM_MESSAGES=32
# MAX_LLM_PROMPT_BYTES=131072
# MAX_LLM_MAX_TOKENS=4096
# MAX_ATTACK_REQUEST_BYTES=65536
# MAX_ATTACK_RESPONSE_BYTES=131072
